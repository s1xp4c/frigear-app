name: 'tests'
on:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: supabase/setup-cli@v1
        with:
          version: latest
      - run: supabase start
      - uses: pnpm/action-setup@v4
        with:
          version: 'pnpm@9.13.1+sha256.dd98691b127b5d2c4d0605b594e98ec0bcbe5fba86358184de706c3312a4e2ee'
      - run: pnpm install
      - run: cp .env.example .env
      - run: pnpm run test
#        TODO: Add coverage to PR
#      - name: 'Report Coverage'
#        if: always()
#        uses: davelosert/vitest-coverage-report-action@v2